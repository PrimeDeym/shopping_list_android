<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ua.primedeym.shoppinglist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ListsActivity"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ShoppingListActivity"
            android:parentActivityName=".ListsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".NoteActivity"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".CreateNoteActivity"
            android:parentActivityName=".NoteActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".NoteResultActivity"
            android:parentActivityName=".NoteActivity"></activity>
    </application>

</manifest>
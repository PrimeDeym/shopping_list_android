<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_create_note_actvity"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ua.primedeym.shoppinglist.Note.CreateNoteActivity">

    <Button
        android:text="Изменить"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/button_note_edit"
        android:onClick="editNote"
        android:elevation="0dp"
        android:layout_alignBottom="@+id/activity_edit_title"
        android:layout_alignParentEnd="true" />

    <EditText
        android:layout_width="300dp"
        android:inputType="textMultiLine"
        android:ems="10"
        android:id="@+id/activity_edit_description"
        android:hint="Описание заметки"
        android:layout_height="wrap_content"
        android:layout_marginTop="42dp"
        android:layout_below="@+id/activity_edit_title"
        android:layout_alignStart="@+id/activity_edit_title" />

    <EditText
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:id="@+id/activity_edit_title"
        android:hint="Название заметки"
        android:layout_marginTop="35dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />
</RelativeLayout>

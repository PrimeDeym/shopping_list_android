<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_create_note_actvity"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ua.primedeym.shoppinglist.Note.CreateNoteActivity">

    <EditText
        android:id="@+id/et_note_title"
        android:layout_width="190dp"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignStart="@+id/et_note_description"
        android:ems="10"
        android:hint="Название заметки"
        android:inputType="textPersonName" />

    <Button
        android:id="@+id/button_note_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/et_note_title"
        android:layout_alignBottom="@+id/et_note_title"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="13dp"
        android:elevation="0dp"
        android:onClick="addNote"
        android:text="Добавить" />

    <EditText
        android:id="@+id/et_note_description"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/button_note_add"
        android:layout_below="@+id/button_note_add"
        android:ems="10"
        android:hint="Описание заметки"
        android:inputType="textMultiLine" />
</RelativeLayout>
